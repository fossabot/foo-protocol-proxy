// Package version to set the current version of the application.
package version

// VERSION is the app-global version string, which should be substituted with a real value during build.
var VERSION = "{{VERSION}}"
